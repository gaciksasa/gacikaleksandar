<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /gacikaleksandar/

    # Redirect to clean URL
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^article/([a-zA-Z0-9_-]+)$ view_article.php?slug=$1 [L]
</IfModule>
